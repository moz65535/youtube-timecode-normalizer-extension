# 実データテスト

外部Wikiから取得した実データは、著作権および再配布への配慮からこのリポジトリには収録していません。

ローカルで実データテストを行う場合は、次のいずれかへデータを配置します。

- `test/cases/real/`
- リポジトリの隣にある `work/private-test-data/real/`
- 環境変数 `REAL_TEST_DATA_DIR` で指定したディレクトリ

対応するファイル形式は `.txt`、`.html`、`.htm` です。データが存在しない場合、実データテストは `SKIP` と表示されます。
